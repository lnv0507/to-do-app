spring.application.name=Anhemsoftware-license-app
spring.profiles.active=dev
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/anhemsoftware-license-app}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:khoi123456}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# RabbitMQ
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:guest}

# Redis
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}

# Multipart file upload limit
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

#mail
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=leminhkhoi16062k5@gmail.com
spring.mail.password=idmk uqns obue gnun
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
#AWS S3 Configuration
aws.access-key-id=AKIAUJEM2GRJ656I7E5N
aws.secret-access-key=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
aws.region=ap-southeast-1
aws.s3.bucket-name=amz-s3-demo-294500578387

# Disable Spring Boot Docker Compose integration in local runs
spring.docker.compose.enabled=false
